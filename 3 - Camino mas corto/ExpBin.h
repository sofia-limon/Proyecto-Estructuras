#pragma once
#include "Graph.h"

void startExpBin(Graph& g);

/*
En esta funcion podemos encontrar la distancia minima entre cualesquiera dos nodos en
un grafo con pesos con exactamente k aristas intermedias.

La complejidad de esta Exponenciacion Binaria de Matrices es de O(V^3 * log(k)).
*/